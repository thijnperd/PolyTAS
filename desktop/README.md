# PolyTAS Desktop

Electron-based desktop app for PolyTAS with an embedded game view and live replay preview.

## Run (dev)
```bash
cd desktop
npm install
npm run dev
```

## Notes
- Set the game URL in the right-side Game Preview panel.
- Use RUN REPLAY to play inputs inside the embedded game view.
